function capitalizeSentences(str){
let everySen = ''
for(let i =0; i< everySen.length; i++){
if(everySen.length[i] < everySen.length){
  everySen[i].charAt().toUpperCase();
}
let str= everySen.join('')
}
return str
}

function isValidPassword (str){
for(let i = 0; i < str.length; i++){
  if(str[i] === str.length){
  }else if(str[i] === str[i] + ''){
return false
}else if(str[i] === str.length < 12){
  return false
}else{
return true
}
}
}

function makeHalfSquares(num1){
  let halve = []
  for(let i= 0; i < num1.length; i++){
if (halve[i] === num1.length){
  }else if(){

  }

  }


    function countAs(grade){
for(let i = 0; i < grade.length; i++){

}
    }

  }
    function deleteMiddleLetters(){

    }

    function lastIndexOfSpace(){

    }

    function hyphenateName(){

    }


if (typeof capitalizeSentences === 'undefined') {
  capitalizeSentences = undefined;
}

if (typeof isValidPassword === 'undefined') {
  isValidPassword = undefined;
}

if (typeof makeHalfSquares === 'undefined') {
  makeHalfSquares = undefined;
}

if (typeof countAs === 'undefined') {
  countAs = undefined;
}

if (typeof deleteMiddleLetters === 'undefined') {
  deleteMiddleLetters = undefined;
}

if (typeof lastIndexOfSpace === 'undefined') {
  lastIndexOfSpace = undefined;
}

if (typeof hyphenateName === 'undefined') {
  hyphenateName = undefined;
}


module.exports = {
  capitalizeSentences,
  isValidPassword,
  makeHalfSquares,
  countAs,
  deleteMiddleLetters,
  lastIndexOfSpace,
  hyphenateName,
}
